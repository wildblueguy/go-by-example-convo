## ▶️ **2.8 — Recursion**

**Task:**
Write a complete Go program that:

* Implements a **recursive function** to compute the **factorial** of an integer `n`
* Computes the factorial of `5`
* Prints the result

**Constraints (per Go by Example):**

* Use recursion (no loops for the computation)
* Use `fmt.Println`
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **2.8**.
