## ▶️ **2.6 — Functions (Variadic & Multiple Return)**

**Task:**
Write a complete Go program that:

* Defines a function

  ```go
  sumAndCount(vals ...int) (int, int)
  ```

  which returns:

  * the sum of all arguments
  * the count of arguments
* Calls this function with arguments `1, 2, 3, 4`
* Prints **both** returned values

**Constraints (per Go by Example):**

* Use a **variadic parameter**
* Use **multiple return values**
* Use `fmt.Println`
* Program must compile and run

**Submit a complete Go file**, for example:

```go
package main

import "fmt"

func main() {
    // your code
}
```

⏳ Awaiting your submission for **2.6**.
