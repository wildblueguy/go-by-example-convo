## ▶️ **5.20 — Base64 Encoding & Decoding**

**Task:**
Write a complete Go program that:

* Encodes the string `"golang"` using **Base64**
* Decodes the encoded value back to the original string
* Prints **both** the encoded and decoded values

**Constraints (per Go by Example):**

* Use `encoding/base64`
* Use `fmt.Println`
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **5.20**.
