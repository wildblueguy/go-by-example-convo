## ▶️ **5.22 — Templates**

**Task:**
Write a complete Go program that:

* Uses `text/template`
* Defines a template that generates:

  ```html
  <h1>Hello {{.Name}}</h1>
  ```
* Executes the template with data where `Name = "Gopher"`
* Prints the generated output

**Constraints (per Go by Example):**

* Use `text/template`
* Use `template.New` or `template.Parse`
* Execute the template to standard output
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **5.22**.
