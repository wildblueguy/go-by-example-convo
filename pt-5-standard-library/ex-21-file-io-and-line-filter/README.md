## ▶️ **5.21 — File I/O & Line Filter**

**Task:**
Write a complete Go program that:

* Reads lines from **standard input**
* Converts each line to **uppercase**
* Prints the transformed lines to **standard output**

**Constraints (per Go by Example):**

* Use `bufio.Scanner`
* Use `os.Stdin`
* Use `strings.ToUpper`
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **5.21**.
