## ▶️ **5.25 — Process & Environment Variables**

**Task:**
Write a complete Go program that:

* Looks up an **environment variable**
* If the variable is set, prints its value
* If the variable is **not set**, prints:

  ```
  not set
  ```

**Constraints (per Go by Example):**

* Use `os.LookupEnv`
* Use `fmt.Println`
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **5.25**.
