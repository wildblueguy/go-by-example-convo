## ▶️ **5.23 — HTTP Client & Server**

**Task:**
Write a complete Go program that:

* Starts an **HTTP server** that responds with `"Hi"` on path `/`
* Makes an **HTTP client request** to that server
* Prints the response body

**Constraints (per Go by Example):**

* Use `net/http`
* Use `http.HandleFunc`
* Use `http.Get`
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **5.23**.
