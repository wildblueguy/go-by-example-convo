## ▶️ **5.19 — SHA256 Hash**

**Task:**
Write a complete Go program that:

* Computes the **SHA256 hash** of the string `"gobyexample"`
* Prints the hash in **hexadecimal format**

**Constraints (per Go by Example):**

* Use `crypto/sha256`
* Use `fmt.Printf` or `fmt.Println` with proper formatting
* Output must be the hex-encoded hash
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **5.19**.
