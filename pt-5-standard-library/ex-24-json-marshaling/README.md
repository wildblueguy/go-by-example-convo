## ▶️ **5.24 — JSON Marshalling**

**Task:**
Write a complete Go program that:

* Defines a struct:

  ```go
  type User struct {
      Name  string
      Email string
  }
  ```
* Creates a `User` value
* Marshals it to **JSON**
* Prints the resulting JSON string

**Constraints (per Go by Example):**

* Use `encoding/json`
* Use `json.Marshal`
* Use `fmt.Println`
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **5.24**.
