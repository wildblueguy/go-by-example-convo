## ▶️ **4.16 — Worker Pools**

**Task:**
Write a complete Go program that:

* Implements a **worker pool** with **3 workers**
* Sends **jobs** (integers) on a channel
* Each worker **doubles** the job value
* Sends results back on another channel
* Prints the results

**Constraints (per Go by Example):**

* Use goroutines and channels
* Use separate job and result channels
* Use `fmt.Println`
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **4.16**.
