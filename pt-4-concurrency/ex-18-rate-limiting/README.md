## ▶️ **4.18 — Rate Limiting**

**Task:**
Write a complete Go program that:

* Processes **5 requests**
* Uses `time.Tick` to **rate limit** processing to **one request every 200ms**
* Prints each request as it is processed

**Constraints (per Go by Example):**

* Use `time.Tick`
* Do not use `time.Sleep` for the rate limiting
* Use `fmt.Println`
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **4.18**.
