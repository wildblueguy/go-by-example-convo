## ▶️ **4.17 — WaitGroups & Atomic Counters**

**Task:**
Write a complete Go program that:

* Uses a `sync.WaitGroup`
* Uses `sync/atomic` to safely increment a shared counter
* Launches **50 goroutines**
* Each goroutine increments the counter by **1000**
* Waits for all goroutines to finish
* Prints the final counter value

**Constraints (per Go by Example):**

* Use `sync.WaitGroup`
* Use `sync/atomic`
* Do **not** use mutexes
* Use `fmt.Println`
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **4.17**.
