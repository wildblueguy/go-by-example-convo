## ▶️ **4.15 — Select & Timeouts**

**Task:**
Write a complete Go program that:

* Uses a `select` statement
* Waits to receive from a channel
* Uses `time.After` to implement a **1-second timeout**
* Prints `"done"` if the timeout occurs before any value is received

**Constraints (per Go by Example):**

* Use `select`
* Use `time.After`
* Use `fmt.Println`
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **4.15**.
