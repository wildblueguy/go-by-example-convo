## ▶️ **4.13 — Goroutines & Channels**

**Task:**
Write a complete Go program that:

* Creates a channel of type `string`
* Starts a **goroutine** that sends `"Hello"` on the channel
* Receives the value in `main`
* Prints it

**Constraints (per Go by Example):**

* Use `go` to start a goroutine
* Use an unbuffered channel
* Use `fmt.Println`
* Program must compile and run without deadlock

**Submit a complete Go file.**

⏳ Awaiting your submission for **4.13**.
