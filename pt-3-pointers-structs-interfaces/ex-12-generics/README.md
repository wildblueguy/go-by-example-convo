## ▶️ **3.12 — Generics**

**Task:**
Write a complete Go program that:

* Defines a **generic function**

  ```go
  max[T constraints.Ordered](a, b T) T
  ```

  which returns the larger of `a` and `b`
* Calls this function with two values of an ordered type
* Prints the result

**Constraints (per Go by Example):**

* Use Go generics (type parameters)
* Use `constraints.Ordered`
* Use `fmt.Println`
* Program must compile with Go 1.18+

**Submit a complete Go file.**

⏳ Awaiting your submission for **3.12**.
