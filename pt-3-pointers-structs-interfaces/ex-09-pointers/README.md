## ▶️ **3.9 — Pointers**

**Task:**
Write a complete Go program that:

* Declares an `int` variable
* Takes a **pointer** to it
* Modifies the value **via the pointer**
* Prints **both** the original variable and the dereferenced pointer

**Constraints (per Go by Example):**

* Use `&` and `*`
* Use `fmt.Println`
* Program must compile and run

**Submit a complete Go file.**

⏳ Awaiting your submission for **3.9**.
